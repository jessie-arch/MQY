spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mqy?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: Qwe721798042
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
auth:
  token:
    secret-key: mqy-local-jwt-secret-hhr-hy-zsy-lm